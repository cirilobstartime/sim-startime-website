limit_req_zone $binary_remote_addr zone=simf_login:10m rate=5r/m;
limit_conn_zone $binary_remote_addr zone=simf_connections:10m;

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name sim.startime.sa;

    ssl_certificate /etc/letsencrypt/live/sim.startime.sa/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sim.startime.sa/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    server_tokens off;
    client_max_body_size 70m;
    gzip on;
    gzip_comp_level 5;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_vary on;
    gzip_types
        application/javascript
        application/json
        application/ld+json
        application/manifest+json
        application/xml
        image/svg+xml
        text/css
        text/plain
        text/xml;
    # HTTP/2 counts concurrent requests independently. Image-heavy pages can
    # legitimately exceed 30 while loading without representing abusive use.
    limit_conn simf_connections 120;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    location = /api/cms-users/login {
        limit_req zone=simf_login burst=3 nodelay;
        proxy_pass http://127.0.0.1:3004;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_hide_header X-Powered-By;
    }

    location / {
        proxy_pass http://127.0.0.1:3004;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 90s;
        proxy_hide_header X-Powered-By;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name www.sim.startime.sa;

    ssl_certificate /etc/letsencrypt/live/sim.startime.sa/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sim.startime.sa/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    return 301 https://sim.startime.sa$request_uri;
}

server {
    listen 80;
    listen [::]:80;
    server_name sim.startime.sa www.sim.startime.sa;
    return 301 https://sim.startime.sa$request_uri;
}
